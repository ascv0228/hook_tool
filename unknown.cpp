#include <jni.h>
#include "hook.h"
#include <Logger.h>
#include <pthread.h>
// UserDBManager_AddTrophy

Config::isbackgroundHack = true;
    while (!il2cpp_handle) {
        sleep(1);
    }
    while (!il2cpp_handle) {
        sleep(1);
    }
    sleep(3);
    
// hack_thread
// android_get_device_api_level
// adb install --abi armeabi-v7a rd.apk
